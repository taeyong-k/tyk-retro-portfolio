# 📘 프로젝트 소개  

## ⚙️ 프로젝트 명

**포트폴리오 (레트로)**

*(레트로 스타일 기반 포트폴리오 재개발 2번째 버전)*

- **기간:** 2025.04.01 ~ 2025.10.25
- **목표:** 이전 포트폴리오보다 시각적으로 세련되고 직관적인 UI/UX 구현, 전체적인 레이아웃·애니메이션 최적화, 프론트/백엔드 역량 표현
- **배포 사이트:** https://www.taeyong.dev/

## 🧩 프로젝트 개요

- 이전 포트폴리오의 디자인과 기능적 한계를 개선하고, 시원시원하고 가독성 좋은 레이아웃 목표로 재개발
- 전체 페이지를 원페이지 스크롤 형식으로 구성, 배경 고정 스크롤과 각 섹션별 인터랙티브 요소 적용
- LP판, 비디오 테이프, 레트로 윈도우, 디지털 레트로 장치 등 테마 활용
- 프론트엔드와 백엔드 모두 고려한 포트폴리오 구조 설계

## 🛠 사용 기술

### 🔧 개발 환경

- **Backend:** Java, Spring Boot, Spring MVC, JDBC
- **Frontend:** HTML, CSS/SCSS, JavaScript, Thymeleaf
- **Database:** MariaDB

### ⚙️ 프레임워크 / 라이브러리

- **프레임워크:** Spring Boot, Spring MVC, MyBatis
- **라이브러리:** GSAP (애니메이션 + Draggable), Font Awesome, Ionicons, Lombok

### 🛠 도구 / 기타

- **IDE / 빌드:** IntelliJ, Maven, JDK, WAR
- **버전 관리:** Git, GitHub
- **기획/문서:** Notion
- **폰트 / 타이포그래피:** Press Start 2P, NeoDunggeunmo, DotGothic16, IBM Plex Sans KR

## ☁️ 배포 환경

- **서버 호스팅:** Google Cloud
- **HTTPS / DNS:** Cloudflare
- **이미지 호스팅:** Gabia

## 🔑 주요 기능 및 특징

1. **인트로 (표지)**
    - CRT + 타이핑 효과 기반 애니메이션, 로딩창과 전원 켜짐 효과
    - 최적화 문제로 비디오 기반 인트로로 대체, 모바일 반응형 적용
2. **툴바**
    - 스크롤 고정 상단 툴바, 각 섹션 아이콘 클릭 시 부드럽게 이동
    - GitHub, 이메일 버튼 포함, 커스텀 커서와 연동
3. **어바웃미**
    - 레트로 윈도우 98 창 스타일, 정보 박스 구조
    - 사진, 인터뷰, 자격증, 연락처(티스토리, 노션, GitHub) 포함
    - 모바일 반응형, 텍스트 가독성 강화
4. 커리어 (픽셀 레트로 디자인)
    - 레트로 테마 느낌만 살리고, 디스크/테이프/LP 이미지 그대로 재현은 하지 않음
    - HTML/CSS로 디지털 레트로 느낌 구현, 정보는 간결하게 나열
    - 레트로 윈도우 스타일 참고, 회사·기간·업무 세로 정렬
    - Jello 애니메이션 적용, 가독성·간결성 최우선
5. 스킬 (LED 전광판 스타일)
    - nav 버튼 기반 LED 보드, 텍스트 JS 렌더링
    - 최대 6개 항목 표시, 초과 시 줄바꿈/스크롤 적용
    - 자동 전환 + 사용자 활동 감지 → 정지 후 재시작 (총 8초)
    - 반응형 처리: aspect-ratio, clamp, media query 활용
    - 디자인/렌더링 문제 다수 해결, 과부하 최소화
6. **프로젝트**
    - LP판 인터랙티브 갤러리 스타일 (리듬 게임 스타일에서 영감을 받음)
    - 왼쪽 영역: LP판 원형 이미지 갤러리, 스크롤로 선택 가능
    - 오른쪽 영역: 프로젝트 정보, 스크린샷 슬라이드, 보기/깃허브 버튼
    - Block Reveal 텍스트 애니메이션, 그라디언트 보더 효과, 모달 확대 보기 적용
    - 반복 조건 및 과부하 방지 로직 적용
7. **푸터**
    - confetti 이펙트 적용(과부하 방지 설정 반영)
    - 텍스트 이펙트 한국어 대응 커스텀
    - 푸터 영역 10% 노출 시 애니메이션 시작 및 초기화

---

| 섹션 | 특징 |
| --- | --- |
| 인트로 | CRT + 타이핑 효과, 비디오 대체, 모바일 반응형 |
| 툴바 | 상단 고정, 아이콘 클릭 smooth scroll, 커스텀 커서 연동 |
| 어바웃미 | 레트로 윈도우 스타일, 사진·자격증·연락처 포함, 반응형 |
| 커리어 | 디지털 레트로 느낌, 정보 간결 정리, Jello 애니메이션 |
| 스킬 | LED 보드, 최대 6개 표시, 자동 전환·정지, 반응형 |
| 프로젝트 | 인터랙티브 갤러리, 스크린샷 슬라이드, Block Reveal, 모달 확대 |
| 푸터 | confetti 이펙트, 텍스트 애니메이션, 노출 시 애니메이션 트리거 |

---

## 🔥트러블 슈팅

### 1. LocomotiveScroll + 툴바 이동 스크롤 튐 문제

- **문제점**
    - 툴바 버튼 클릭 후 섹션 이동 직후, 스크롤하면 이전 위치로 순간이동.
    - 원인: LocomotiveScroll의 smooth 보간값과 브라우저 native scroll 위치가 불일치.
- **해결**
    - ScrollSmoother로 교체, `scrollTop`과 내부 스무딩 값을 JS로 동기화.
    - 버튼 클릭 시 `scrollSmoother.scrollTo(target, {duration: 0.5, offset: 0})`로 위치 강제 설정.
    - 이동 중 다른 scroll 이벤트를 무시하도록 상태 플래그(`isScrolling`) 추가.
- **회고**
    - smooth scroll 엔진의 내부 좌표와 DOM scroll 위치 동기화 중요성 확인.
    - 외부 라이브러리 적용 시 edge case를 예상하고 이벤트 플래그로 제어 필요.


### 2. 모달 닫을 때 애니메이션 재실행 문제

- **문제점**
    - 모달 재오픈 시 애니메이션 중복 실행 및 scroll 위치 초기화.
    - 원인: ScrollSmoother의 스크롤 상태와 모달 이벤트 충돌.
- **해결**
    - 모달 열기 전 `scrollSmoother.paused(true)`, 닫기 후 `scrollSmoother.paused(false)`로 잠금/해제.
    - 애니메이션 트리거를 `once: true` → 재실행 시 상태 초기화(`animation.restart()`)로 변경.
- **회고**
    - UI 이벤트와 스크롤 상태 동기화 없이는 반복 컴포넌트 안정적 동작 어려움.
    - 상태 플래그와 라이프사이클 관리가 필수적임.

### 3. LED 보드 반응형 문제

- **문제점**
    - 화면 축소 시 dot 크기와 텍스트 비율 깨짐, CSS만으로 유지 어려움.
- **해결**
    - CSS `aspect-ratio`로 dot 가로세로 비율 유지, JS로 동적으로 컬럼 수 계산(`Math.floor(containerWidth / dotSize)`), dot position과 text 위치 재계산.
    - resize 이벤트마다 재렌더링하여 레이아웃 깨짐 방지.
- **회고**
    - 동적 레이아웃은 CSS + JS 협업 필요.
    - 단순 CSS는 비율 유지 불가, JS로 container width 기반 재계산 필수.

### 4. 커서 위치 문제 (중앙 정렬)

- **문제점**
    - 커서가 화면 중앙 기준으로 정확히 위치하지 않음.
    - 원인: pointermove 이벤트 기준 좌표와 transform-origin 불일치.
- **해결**
    - JS에서 커서 좌표(`clientX/Y`) 가져와 `transform: translate(-50%, -50%)` 적용.
    - 이벤트 초기화 시점 DOM 크기 계산 후 중앙 기준 재정렬.
    - window resize 시 `updateCursorPosition()` 호출해 위치 유지.
- **회고**
    - UI 디테일 조정은 좌표 기준과 DOM 렌더링 시점 이해 필수.
    - 작은 오차도 UX에 큰 영향.


### 5. 오프닝 타이핑/애니메이션 색상 충돌

- **문제점**
    - 타이핑 애니메이션과 배경색 겹침으로 글자 가시성 저하.
    - z-index와 애니메이션 속성 충돌.
- **해결**
    - 애니메이션 레이어 구조 재설계: `typingLayer`와 `backgroundLayer` 분리.
    - z-index: `typingLayer: 10`, `backgroundLayer: 1`로 명확히 지정.
    - GSAP timeline에 `opacity`, `color` 속성만 적용하고, 불필요한 transform/scale 속성 제거.
    - 애니메이션 순서 제어: background fade-in → typing 시작 → 글자 색상 transition.
- **회고**
    - 레이어 구조와 애니메이션 속성 충돌 최소화 필요.
    - 초기 설계 단계에서 구조적 분리와 timeline 순서 제어가 안정적 UI 핵심.


### 6. 갤러리 애니메이션 위치 어긋남 문제

- **문제점**
    - 갤러리 애니메이션 실행 중, 크롬 브라우저에서 이미지가 원래 위치보다 아래로 어긋나는 현상이 발생함.
    - 엣지에서는 동일 환경에서도 문제 없이 정상 작동.
    - 원인: `runAnimation()`에서 모든 이미지의 `x`와 `y` 좌표를 0으로 초기화하면서, 원형 배치용 `transformOrigin` 기준 좌표가 변해 시각적으로 이미지가 아래로 내려오는 현상이 발생함.
- **해결**
    - runAnimation과 resetAnimation에서 `x`와 `y` 초기화를 제거하고, `rotation`만 초기화하도록 변경.
    - 이렇게 하면 transformOrigin 기준으로 원형 배치가 유지되어 위치 튐 현상이 사라짐.
- **회고**
    - 초기 애니메이션과 리셋 시 불필요한 좌표 초기화는 `transform` 구조를 깨뜨려 브라우저 간 동작 차이를 유발할 수 있음.
    - 최소한의 속성만 초기화하도록 설계하면 모든 브라우저에서 안정적으로 작동함을 확인.

## 🎯 결과 및 성과

- **최대한의 완성도 달성:** 수개월간 집중 개발, 디테일 하나하나까지 손수 구현. 전체 UI/UX, 애니메이션, 레이아웃, 반응형 대응 모두 직접 점검하고 조정.
- **기술적 도전 극복:** 프로젝트 섹션은 내 기술 범위를 넘어서는 수준까지 구현 → 스크롤 기반 인터랙티브 갤러리, Block Reveal 텍스트, 이미지 슬라이드 + 모달 확대, 버튼 애니메이션 등 복합 요소 안정화.
- **참고와 재해석:** 기존 애니메이션/인터랙션 코드를 참고하여 내 구조에 맞게 재구성 → 불필요 기능 제거, 핵심만 남겨 최적화.
- **반응형/모바일 완전 대응:** 모든 섹션 화면 크기별 점검, 커서·툴바·스크롤 연동 문제 해결, 갤러리 자동 중앙 정렬, 정보 라인 스크롤 동기화 등 조정.
- **디테일 집중:** 단순 구현이 아닌 시각적 완성도와 UX 고려 → 그라디언트 배경, 이미지 슬라이드, 버튼 빛나는 효과 등 세밀한 연출 반영.
- **성과 요약:** 기술적 한계와 버그를 극복하며 최적화, 클린코드, 인터랙티브 UX 완성 → 결과적으로 사용자 경험과 시각적 완성도 모두 만족.

